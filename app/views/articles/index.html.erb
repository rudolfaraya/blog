<h2> Lista de Artículos:</h2>
<% @articles.each_with_index do |article, index|  %>
  <h1><%= "#{index + 1}" %>. <%=  link_to article.title, article %></h1>
  <div>
    <%= article.body %>
  </div>
<% end %>
<%= will_paginate @articles %>